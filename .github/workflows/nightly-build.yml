name: Nightly ISO Build

# Fork build branch: dispatch only. This branch is the fork's default branch, so a
# schedule here would build nightly -- and its artifact cleanup below would delete the
# fork ISO a dispatched run just uploaded.
on:
  workflow_dispatch: # Allow manual triggering

jobs:
  build-iso:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@v1.3.1

      - name: Build ISO
        run: NO_BOOT_OFFER=1 ./bin/omarchy-iso-make

      - name: Delete previously uploaded ISO artifact
        uses: philips-labs/action-delete-artifacts@v1.0.0
        with:
          workflow: nightly-build.yml
        continue-on-error: true

      - name: Upload newly built ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: omarchy-iso-${{ github.run_number }}
          path: ./release/*.iso
          retention-days: 3 # fork build: long enough to download a 6 GB artifact at leisure
